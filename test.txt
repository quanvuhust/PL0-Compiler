Program Example2; 
CONST N = 10, M=5, P=6000;
Var a,b,c,g[2],t;
Procedure gcd(a; b; var d; c);  
	Begin    
	   	While a <> b do
			if a > b + 3*a then a := a - b
			else b := b - a;	
	  	d := b;
		BEGIN
			a := M;
			BEGIN
				c := N
			END;
		END;
	End;
Begin  
	g[1] := 3;
   	Call Readln(a);	Call Readln(b);
	Call gcd(a,b,c, t);
	Call Writeln(c);
End.
